VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional: service role key (server-side only). Do NOT expose in client builds.
# VITE_SUPABASE_SERVICE_ROLE_KEY=

# Usage: copy this file to .env.local (or set platform env vars) and fill values.